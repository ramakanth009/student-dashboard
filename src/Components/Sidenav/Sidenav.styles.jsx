import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    sidenav: {
        width: '250px',
        height: '100vh',
        backgroundColor: '#f0f0f0',
      },
      sidebarlogos:{
        paddingRight:"10px"
      }
}))